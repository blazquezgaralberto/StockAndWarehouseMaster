server.port=8080
spring.application.name=stockAndWarehouseMaster
server.servlet.context-path=/stockAndWarehouseMaster

## DATASOURCE
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/stockAndWarehouseMaster}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root1234}

## LIQUIBASE
spring.liquibase.change-log=classpath:database/db.changelog-master.xml

## JWT
stockAndWarehouseMaster.app.jwtSecret=${JWT_SECRET:miClaveSecretaSuperSeguraParaDesarrollo1234567890}
stockAndWarehouseMaster.app.jwtExpirationMs=86400000

## MAIL SENDER
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:alberto10maco@gmail.com}
spring.mail.password=${MAIL_PASSWORD:usmrfsssbwncgcki}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

## SUBIDA DE ARCHIVOS
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

## MYBATIS
mybatis.mapper-locations=classpath:/mappers/*.xml